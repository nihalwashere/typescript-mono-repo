{
  "name": "winuall",
  "version": "1.0.0",
  "description": "All things web for connect frontend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "storybook": "lerna run storybook --stream",
    "build-lib": "lerna run build-lib --stream",
    "pre-start": "yarn build-lib",
    "start": "lerna run start --stream"
  }
}
